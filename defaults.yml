verbose: 0
version: false
debug:
  cfg: false
workdir:
  use: false
  dir: "./runtime2/"
broadcast:
  station_ids:
    csv: "default"
    pkey: "openmedia_id"
cfgi:
  file: "./tests/defaults_alt.yml"
commands:
  station:
    filter:
  show:
    filter:
apis:
  common:
    station_ids:
    csv: "default"
    pkey: "openmedia_id"
  croapp:
    urls:
      mock: "https://mockservice.croapp.cz/mock"
      doc: "https://rapidoc.croapp.cz"
      api: "https://rapidev.croapp.cz"
    update_local_db: true
    # update_local_db: "daily"
    # update_local_db: [true, false, daily]
    # filter: ""
    # stations: 
      # id: "11"
    endpoints: [
      "stations-all",
      "stations",
      "shows",
      "serials",
      "schedule",
      "program",
      # "schedule-day",
      # "schedule-day-flat",
      # "schedule-current",
      "episodes",
      "genres",
      "persons",
      "topics",
      "keywords",
    ]

test:
  par: "def"
  env: "def"
  def: "def"
  alt: "def"
  cfg_prio: "def.yml"
  logs: false
