verbose: 0      # -v; int; count; log level;
version: false  # -V; bool; store_true; print version of program
debug:          #debug_lv0_line1
  cfg: false    # -dc; bool; store_true; debug runtime config
workdir:
  dir: "./runtime/"
cfgi:
  file: "./tests/defaults_alt.yml"
commands: # subcommands
  # station_ids:
  station: # filter stations
    id:     # -id; int; store; filter by id
    test: 
  show:
apis:
  common:
    station_ids:
    csv: "default"
    pkey: "openmedia_id"
  croapp:
    workdir:
      dir: "./runtime/croapp/"
    urls:
      mock: "https://mockservice.croapp.cz/mock"
      doc: "https://rapidoc.croapp.cz"
      api: "https://mujrozhlas.croapi.cz"
      swagger: "https://rapidoc.croapp.cz/apifile/openapi.yaml"
    DB_local: 
      csvs_update: true
      #csvs_update: [true, false, daily]
      csvs_workdir: "./runtime/croapp/csv/"
    response:
      limit: 500
      limit_str: "?page%5Blimit%5D="
    endpoints: [
      "stations-all",
      "stations",
      "shows",
      "serials",
      "schedule",
      "program",
      # "schedule-day",
      # "schedule-day-flat",
      # "schedule-current",
      "episodes",
      "genres",
      "persons",
      "topics",
      "keywords",
    ]

# FOR TESTING
## DEBUGING CONFIG FILE
test:
  par: "def"      # -tp; none; store_true; test par
  env: "def"      # -te; none; store_true; test env
  def: "def"      # -td; none; store_true; test def
  alt: "def"      # -ta; none; store_true; test alt
  cfg_prio: "def.yml"
  logs: false
    
## TESTING PARAMS DEFINITION
dbool: false      # -db; none; store_true; dummy boolean;
dint: 10          # -di; int; store; dummy int;
